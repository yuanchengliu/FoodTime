<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.foottime.receptiodesk.mapper.ClassifictionMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.foottime.receptiodesk.entity.Classifiction">
        <result column="bid" property="bid" />
        <result column="bname" property="bname" />
        <result column="option" property="option" />
        <result column="fsort" property="fsort" />
        <result column="sort" property="sort" />
        <result column="tsort" property="tsort" />
        <result column="cid" property="cid" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        bid, bname, option, fsort, sort, tsort, cid
    </sql>

</mapper>
